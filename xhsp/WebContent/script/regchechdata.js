/**
 * 
 */

function chechdata() {
	var ssn = form.username.value.toLowerCase();
	if (!che) {

	}

}

function checkUseName(ssn) {
	if (ssn.length < 6 || ssn.length > 18) {
		alert("\请输入正确的用户名，用户名长度为3-18位！")
	}
}

